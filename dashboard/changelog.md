# House change log

Edits to the *rules*, not to the entries. When the standard itself changes — the validator,
the house voice, what `keep` is for, what counts as a source — write it down here with the
date and the reason.

Entry-level history lives in git. Corrections live in the colophon. This file is for the
things that would otherwise quietly drift.

| Date | What changed | Why |
|---|---|---|
| 2026-09-01 | The site was split from one hand-edited 636 KB file into `content/` + a build. | Every part became separately updatable, and the house rules became machine-checkable. |
| 2026-09-01 | Two rules moved from CLAUDE.md into hooks: `dist/` is unwritable by hand, and `corrections.json` can only be appended to. | An instruction is followed most of the time. The colophon's promise to readers needs better than that. |
| 2026-09-01 | `/press-publish` set to `disable-model-invocation: true`. | Publishing is irreversible and outward-facing. It should require a person asking for it. |
| 2026-09-01 | Every count written in prose — the meta description and the three wing intros — now comes from the build (`{{N_BOOKS}}`, `{{W_LIVES_CAP}}` etc.). | The hand-written 'twenty ideas' and 'thirty-one sources' were both wrong the moment the shelves grew. |
| 2026-09-15 | Every content field is described in `schemas/`; the editor shows it on hover, and `npm run check` now fails on any field name the schemas do not list. Broken JSON is reported by line and column with the likely fix, in the terminal and in the preview. | So the Press can be maintained by its owner without reading code. A misspelt `chnaged` was valid JSON that the page silently dropped — the one kind of mistake nothing caught. |
| 2026-09-01 | House decision: a Life stays plateless when the only available image carries a licence its uploader likely could not grant. First applied to Henrietta Lacks. | For an entry about consent, a family photo on a shaky licence would undercut the page. The press mark is the honest fallback. |
| 2026-09-18 | The famous/obscure split in the Lives card and the Lives wing intro now comes from the build too (`{{W_FAMOUS_CAP}}`, `{{N_OBSCURE}}`). | The 09-01 rule missed it. "Twenty-one famous, five you have never heard of" was typed by hand and went stale the day Jobs and Moses shipped; it said 21 and 5 while the shelf held 23 and 5. |
| 2026-09-18 | The front door gained three blocks drawn entirely from `content/`: the four parts of an entry shown on a real life, the four newest lives, and a wall of every portrait. | A first-time reader met a mission statement and five wing names, and nothing to hold. The method is the product; showing it on a real entry explains it faster than the colophon does. Built from content so none of it can go stale. |
| 2026-09-21 | Every title and life also builds as its own page (`dist/t/<id>/`, `dist/l/<id>/`); the sitemap lists all of them. | The library routes by hash, so search engines and link previews saw one page. Nothing on the shelves could be found or shared as itself. |
| 2026-09-21 | Atlas `kept` lines: a bare string now prints as a note, "after <name>", labelled as not a quotation; only `{ k, s }` prints as a quotation. The check stopped warning on bare strings. | 104 unsourced lines printed as quotations, breaking a colophon promise until a backfill only Luca can do. The page now tells the truth in the meantime. |
| 2026-09-21 | The Log added: the editor's signed column, Markdown in `content/log/`, with `/log/`, `feed.xml`, `npm run new log` and `/press-log`. `PUBLISHING.md` §2 and §3 amended. | Luca's decision to run the Press as a business with himself as named editor. Opinion is allowed here because it is signed; figures, quotations and corrections are held to the same rules. |
| 2026-09-21 | `content/about.md`: the About page, in Markdown, built only once the editor's section no longer says TODO. | A personal brand needs a person on it; a placeholder that printed would be worse than no page. |
| 2026-09-21 | Plates served as files, not base64. Front door 932 KB → 400 KB; 500 KB budget printed by the build. | The one-file premise ended with the entry pages; see the Retired row in commissions.md. |
| 2026-09-21 | The share card records what it printed (`assets/og.counts.json`); `npm run check` warns when the shelves have moved and the card has not. | The card said 21 titles and 32 lives while the shelves held 22 and 43, on every shared link. |
| 2026-09-21 | `content/newsletter.json`: a sign-up form that prints only once a service is named, and a colophon line saying who holds the addresses. | A reader could not follow the Press. The colophon's privacy sentence has to stay true when a form appears. |
| 2026-09-21 | Custom domain: the deploy passes the `SITE_URL` repository variable to the build, which writes CNAME. | So buying a domain is one setting, not an edit. |
| 2026-09-21 | Seven lives sourced from documents opened in three `press-researcher` sweeps; six sentences cut back to what their sources say (correction 9). | The colophon promised a named source for every figure; eight lives broke it. Borlaug and the Jobs paternity figure remain, as decisions for Luca. |
| 2026-09-21 | Front-door tally: "sources" → "people". Daily pick drawn from the lives nobody has heard of. | The site uses "source" to mean a document opened; the Atlas count collided with it. The obscure lives are the reason to visit. |
| 2026-09-21 | `npm run ready`: a plain-language launch checklist with one next action. `press-status` leads with it. | The owner had a dozen commands and no single place that said what to do next. |
| 2026-09-21 | `npm run card` also renders a share card per title into `assets/cards/`; entry pages use it; the check warns when a card no longer matches its title. | A shared title previewed as the generic front-door card. |
| 2026-09-21 | Wing II hard-figure rule promoted from warning to error. | Its backlog is cleared: every life's figures are sourced or cut (corrections 9 and 11). |
| 2026-09-21 | New warning: a `facts[].s` line must name a document, in the shape "what it counts: where it was read". | 66 lines named nothing, and two of them were wrong (correction 10). Staged as a warning while Wing I is sourced. |
| 2026-09-21 | The adjacent shelf's `facts` removed. | They were jokes and the house's own counts of its own lists, printed under "By the numbers" — the shape PUBLISHING.md §3 refuses. |
| 2026-09-21 | Entries print their own corrections, via a hand-set `corrected` list the check validates. | A reader of a corrected entry never learned it had been corrected. Matching corrections to entries by name was tried and was wrong both ways, so the link is explicit. |
| 2026-09-21 | Wing I sourcing pass, wave 1: 38 source lines rewritten to name the document read; corrections 12 and 13. Many name Wikipedia — accurate about where the house looked, and weaker than a primary source. | The colophon's first promise. |
| 2026-09-21 | Discoverability: a contents page linking every entry (the library linked none), git-dated sitemap entries, `llms.txt`, fuller descriptions, a verification-file slot at `assets/root/`, full git history in the deploy. | A search engine could reach the entry pages only through the sitemap, and every page claimed to change daily. |
| 2026-09-21 | Design: "Surprise me" demoted from the one solid button; the Log takes that slot once published; "sources" → "people" across the Atlas; a skip link. | A novelty outranked the wings; the Atlas's word collided with the house's meaning of "source". |
| 2026-09-21 | Wing I pass, wave 2 (titles 08–14): 21 lines sourced; three figures corrected or cut (correction 14). | New York's subway cost was understated; the count of printing towns was not in the cited paper; the dark-fibre share had no source. |
| 2026-09-21 | Audit wave 2: six superlatives reworded (correction 15); five unverifiable quotations turned into paraphrase (correction 16); a mislabelled Ritchie citation fixed. | Quotation marks mean the words were seen. |
| 2026-09-21 | Reading lists: 16 books that linked to Wikipedia (four to the author's page) now link a WorldCat lookup; Moore and Riemann link the papers. The check refuses a Wikipedia link not labelled as one. | The heading says "Go to the source". |
| 2026-09-21 | Audits that found nothing wrong, recorded so they are not re-run blind: every life's years against Wikidata (Frances Perkins's 1880 is right; Wikidata's 1882 is not); 40 of 43 biographies matched in Open Library, the other three are a book Open Library lacks, a journal article and an exhibition, each described as such. | |
| 2026-09-21 | Entry pages redesigned for hierarchy: breadcrumbs, an at-a-glance strip (read time, sourced figures, disputes, corrections, last updated), the keep line raised to the top, numbered serif section heads, figure cards, disputes and corrections as callouts (corrections fold to their titles), a side rail on wide screens. | Every section had the same weight, and the parts the house sells — disputes, sources, corrections — looked like everything else. |
| 2026-09-21 | Sharing: a share bar on every entry page and under the keep line, a share button in the library's reader that shares the entry page rather than the #hash. Plain links and the native share sheet; no third-party scripts. | A reader could not share anything, and a copied library address previewed as the front door. |
| 2026-09-21 | Structured data: BreadcrumbList, word count, language, main entity, citations from the reading list, the editor's About page as author URL. | So search results can show the breadcrumb and read the page as an article with sources. |
| 2026-09-21 | The colophon — method, plate credits, the numbered corrections and the AI disclosure — moved off the front door to its own page at `#colophon` (`#corrections` jumps straight to the list). The front door keeps the four promises as one block at its foot, with the live correction count and a link to the proof, and gained a first action in the hero. | The colophon was over half the front page, so a new reader scrolled through plate-licence prose to reach the end. The promises still show on the front door; only the long form moved. Every existing `#colophon` link still lands on it. |
| 2026-09-21 | The house-rules block came off the front door as well; the colophon and corrections are reached from the footer (`Colophon`, `Corrections`) and from every entry page. | Luca asked for the front door to carry the library only. The promises are still printed in full on the colophon page. |
| 2026-09-22 | Every entry, in the library's reader and on its own page, gained the same reading tools from one shared file (`theme/reading.js`): a Share button in the top bar, minutes left and a progress line, an end-of-entry block, "Share this line" on the keep line, and select-a-passage quoting that links to the entry's own page at that passage. | The only share control in the reader was a faint row in the side table; nobody found it. One file for both places, so the two cannot drift apart. |
| 2026-09-22 | House rule: a shared quote is always credited "The Commodore Press, on <entry>", never set under the entry's name alone. | A line the Press wrote about Darwin, printed above "Charles Darwin", reads as Darwin's words: an invented quotation by layout. |
| 2026-09-22 | End of every entry: a "Read next" card, chosen from the entry's first `across` link (next on the shelf only when there is none), the same choice in the reader and on the page. Entries finished in this browser are marked "Read ✓" on the shelves, and a part-read entry offers to take the reader back. | The best hook the house has is the argument between two entries, and it sat in the last section of the page. Reading memory is kept in localStorage only; the colophon's privacy sentence was rewritten in the same change to say so, because it had promised that only the night switch was remembered. |
| 2026-09-22 | Every life has a 1200×630 share card (`assets/cards/l/`): name, dates, field, the lede, and the plate, in the life's livery. Plates under an attribution licence print their credit on the card. `npm run check` warns when a card no longer matches its life, including a replaced plate. | A shared life previewed as a small square portrait with no name on it, while titles got full cards. The credit travels on the card because the colophon does not travel with a shared link. The lede is set in roman, never as a quotation under the name. |
| 2026-09-22 | `npm run card` measures every card in the real fonts and exits 1 when text runs into the footer or a plate into the sea. Lives without a plate print as text cards; CC plates' credits add "cropped and toned"; the credit and stale-card stamp live once, in `tools/cards.mjs`, read by the generator and the check. | A contact sheet of all 43 showed what six samples hid: eight empty frames that read as broken images. At larger type, Wu Chien-Shiung's name wrapped and pushed the lede into the footer; nothing but the new measure noticed. The colophon tells readers the plates were changed; the card now says so too. |
| 2026-09-22 | `npm test`: headless Chrome drives an entry page and the library reader and fails on a broken share link, a quote not credited "The Commodore Press, on <entry>", a non-verbatim quote, a progress line that never finishes, or the two surfaces disagreeing. `ship` runs it after the build. | Every bug in the reading tools had been caught by hand, and `check` reads content, not behaviour. Proven by breaking the credit rule on purpose: the test failed in all eight places it checks. |
| 2026-09-22 | The press mark is drawn once, in `build/mark.mjs`, and filled into the masthead, favicons, reader, end-of-entry ornament and share cards; `npm run check` fails on a hand-drawn copy anywhere else. | It was copied into seven files under a comment asking they change together, and the favicon's pennant had already drifted from the rest. |
| 2026-09-22 | Front door: the arrow keys open the Press shelf only when nothing has focus. | They were taking over every arrow press on the page, including from a focused button or the phone's sideways nav. |
| 2026-09-22 | Esc, tested: it closes the share sheet, then the reader, lets go of a selected passage before anything closes, and with nothing open does nothing — never leaving the page. Esc on a selected passage now drops the selection only (it had been closing the whole reader). | The Esc voyage to relentless.com was removed for good by Luca's decision. A returning voyage fails all four browser runs; so does the old selection bug. Both proven by putting them back on purpose. |
| 2026-09-22 | The Atlas is redrawn as a sea chart ("Islands & Lanes"): principles are islands, people are towns on them, lanes join islands held by the same people, and a Life whose own `across` links to a principle becomes an older port on it. A scroll-driven crossing sails those ports in date order. Code moved to `theme/atlas.js` / `theme/atlas.css`. | The star sky placed people at arbitrary points and joined every pair that shared anything (~290 lines), so nothing on it meant anything. Every mark on the chart is now read from `content/`, and the only links across history are ones an entry has argued for. |
| 2026-09-22 | Atlas people now answer to the editor's own listening notes (the private "People" Google Doc, begun November 2025). Four people with no notes behind them came off (James Sexton, Daniel Broukhim, Reid Hoffman, Brandon Novak); Joe Liemandt and Dovid Lichtenstein came on from the doc. Atlas people can carry `corrected`. | A person earns a star for what they taught the editor, and the notes are the evidence of that. Hoffman's take also credited him with Paul Graham's "do things that don't scale". |
