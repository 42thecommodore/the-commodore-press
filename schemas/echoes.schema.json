{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Echoes \u2014 content/atlas/echoes.json",
  "description": "The same idea, turning up in my notes on different people. Each echo is copied from the People notes doc; nothing here is inferred. Prints first under 'How they connect' and in each person's drawer.",
  "type": "array",
  "items": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "idea",
      "notes"
    ],
    "properties": {
      "idea": {
        "type": "string",
        "description": "The idea they share, in a few words: \"Follow your intuition\"."
      },
      "notes": {
        "type": "array",
        "minItems": 2,
        "description": "At least two people, each with the line from my notes on them.",
        "items": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "who",
            "note"
          ],
          "properties": {
            "who": {
              "type": "string",
              "description": "A name exactly as it appears in people.json."
            },
            "note": {
              "type": "string",
              "description": "The line from my notes on them, spelling fixed. A note, not a quotation."
            }
          }
        }
      }
    }
  }
}
