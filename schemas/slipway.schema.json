{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "The Slipway — content/slipway/slipway.json",
  "description": "Wing V, the trade book of operating lessons.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "convs": {
      "type": "array", "x-means": "convergences", "description": "Convergences: lessons several operators arrived at separately.",
      "items": {
        "type": "object", "additionalProperties": false, "required": ["b", "s"],
        "properties": {
          "b": { "type": "string", "x-means": "lesson", "description": "The convergence, short: \"Get customers first.\"" },
          "s": { "type": "string", "x-means": "gloss", "description": "The gloss beneath it." },
          "who": { "type": "string", "description": "Who converged on it, surnames separated by \" · \"." }
        }
      }
    },
    "book": {
      "type": "array", "x-means": "lessons", "description": "The trade book itself — one lesson per item.",
      "items": {
        "type": "object", "additionalProperties": false, "required": ["t"],
        "properties": {
          "k": {
            "type": "string", "x-means": "topic",
            "examples": ["Why", "Money", "Sales", "People", "Ideas", "Scale", "Risk", "Craft", "Starting", "Marketing", "Self"],
            "description": "The topic. In use: Why, Money, Sales, People, Ideas, Scale, Risk, Craft, Starting, Marketing, Self."
          },
          "t": { "type": "string", "x-means": "text", "description": "The lesson, in a sentence or two." },
          "a": { "type": "string", "x-means": "author", "description": "Credit, written \"after <name>\" — your compressed note of their idea, not their exact words." }
        }
      }
    },
    "chandlery": {
      "type": "array", "description": "The venture notes.",
      "items": {
        "type": "object", "additionalProperties": false, "required": ["b", "s"],
        "properties": {
          "b": { "type": "string", "x-means": "note", "description": "The note, short: \"The pipeline is the job.\"" },
          "s": { "type": "string", "x-means": "gloss", "description": "The gloss beneath it." }
        }
      }
    },
    "note": { "type": "string", "description": "The note on what the trade book is, printed with it." }
  }
}
