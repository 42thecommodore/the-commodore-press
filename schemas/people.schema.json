{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "The people of the Atlas — content/atlas/people.json",
  "description": "Everyone in Wing III. Add a person by copying an existing { … } block.",
  "type": "array",
  "items": {
    "type": "object",
    "additionalProperties": false,
    "required": ["name", "domain", "take"],
    "properties": {
      "name": { "type": "string", "description": "Their name as shown in the Atlas." },
      "domain": {
        "type": "string", "examples": ["found", "market", "mind", "craft", "recov", "inner"],
        "description": "Which domain they sit in — an id from domains.json: found, market, mind, craft, recov or inner."
      },
      "role": { "type": "string", "description": "Who they are, in a line: \"Executive coach; Art of Accomplishment\"." },
      "take": { "type": "string", "description": "Your take on what they teach — a sentence or two." },
      "p": {
        "type": "array", "items": { "type": "string" }, "x-means": "principles",
        "description": "The principles they belong to — ids from principles.json. These draw their lines in the Atlas; with none, they float unconnected."
      },
      "kept": {
        "type": "array",
        "items": {
          "oneOf": [
            { "type": "string", "description": "A bare line. Legacy shape: it carries no source, so the check warns. Prefer { k, s }." },
            {
              "type": "object", "additionalProperties": false, "required": ["k", "s"],
              "properties": {
                "k": { "type": "string", "description": "The line, as they said it. Verbatim, and short — a sentence or two, never a passage." },
                "s": { "type": "string", "minLength": 1, "x-means": ["source", "where", "episode"],
                       "description": "Where you heard it, precisely enough that a reader can go and check: \"Founders #312, 14:20\" or an episode URL." }
              }
            }
          ]
        },
        "description": "Lines you kept from them, printed as 'Lines I kept'. These read as quotations to a reader, so each one owes a source the same way a figure does — use { \"k\": \"the line\", \"s\": \"the episode and timestamp\" }. A compressed note rather than their words is not a kept line: put it in `take`, or write it as \"after <name>\"."
      }
    }
  }
}
